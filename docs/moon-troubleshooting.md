# Moon Setup Troubleshooting (Archived)

The Moon UI has been retired and replaced by the OnceUI-powered deployment wizard served from `deployment/panel/`. Use the new dashboard for install/start/stop flows, live NDJSON logs, and settings management.

For current guidance, follow the deployment panel steps outlined in [docs/deployment.md](./deployment.md). That document covers bootstrap requirements, build commands (`npm run deploy:panel:build`), and the streaming output surfaces available in the OnceUI template.
