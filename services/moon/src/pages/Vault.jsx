import React from 'react';

export default function VaultPage() {
  return (
    <div className="stack page" data-testid="vault-page">
      <h1>Vault</h1>
      <p className="page__text">
        Secure identity and access flows, configure JWT lifetimes, and review the gateway that brokers requests to Redis and
        MongoDB.
      </p>
    </div>
  );
}
